// JS principal do front-end
// Inicialização de plugins, listeners globais, etc.
document.addEventListener('DOMContentLoaded', () => {
  if (window.lucide) {
    window.lucide.createIcons();
  }
});
